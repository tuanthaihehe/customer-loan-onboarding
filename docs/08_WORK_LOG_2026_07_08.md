# Work Log 2026-07-08

## DMN rules rebuild from Rule.xlsx

File source:

```text
C:\Users\NDK\Downloads\Rule.xlsx
```

Excel có 1 sheet `Rule` với 19 rule nghiệp vụ.

## Nội dung đã làm

Đã xóa toàn bộ file `.dmn` cũ trong:

```text
backend/loan-onboarding/src/main/resources/dmn
```

Các file cũ bị xóa gồm các file rule rời rạc như:

```text
customer-age-eligibility.dmn
customer-blacklist-check.dmn
asset-rules.dmn
loan-request-rules.dmn
loan-product-recommendation-rules.dmn
asset-valuation-rules.dmn
...
```

Sau đó tạo lại 4 file DMN nhóm theo giai đoạn nghiệp vụ:

```text
customer-kyc-rules.dmn
asset-loan-product-rules.dmn
application-completion-rules.dmn
submission-operational-rules.dmn
```

## Mapping rule vào file DMN mới

### customer-kyc-rules.dmn

Chứa các decision:

- `customerAgeEligibility`
- `existingCustomerSuggestion`
- `blacklistedCustomerSelectionBlock`
- `otpRequirementDecision`
- `otpVerification`

Tương ứng rule:

- `CUS_AGE_ELIGIBLE`
- `CUS_EXISTING_CUSTOMER_SUGGESTION`
- `CUS_BLACKLISTED_SELECTION_BLOCK`
- `KYC_OTP_REQUIREMENT`
- `KYC_OTP_VERIFIED`

### asset-loan-product-rules.dmn

Chứa các decision:

- `assetNotPledged`
- `supportedAssetType`
- `supportedLoanPurpose`
- `activeLoanProduct`
- `proposedAmountWithinEligibleLimit`
- `productAmountRange`
- `productTenorAllowed`
- `productScoringMatch`

Tương ứng rule:

- `AST_NOT_PLEDGED`
- `AST_SUPPORTED_TYPE`
- `APP_SUPPORTED_LOAN_PURPOSE`
- `PRD_ACTIVE_PRODUCT`
- `APP_PROPOSED_AMOUNT_WITHIN_ELIGIBLE_LIMIT`
- `PRD_AMOUNT_RANGE`
- `PRD_TENOR_ALLOWED`
- `PRD_SCORING_MATCH`

### application-completion-rules.dmn

Chứa các decision:

- `referencePersonRequired`
- `referencePhoneUniqueness`
- `identityIssueDateValid`
- `assetRegistrationIssueDateValid`

Tương ứng rule:

- `APP_REFERENCE_PERSON_REQUIRED`
- `APP_REFERENCE_PHONE_UNIQUE`
- `CUS_IDENTITY_ISSUE_DATE_VALID`
- `AST_REGISTRATION_ISSUE_DATE_VALID`

### submission-operational-rules.dmn

Chứa các decision:

- `ekycVerificationPassed`
- `applicationEditable`

Tương ứng rule:

- `KYC_EKYC_VERIFICATION_PASSED`
- `APP_EDITABLE`

## Code cập nhật

Đã cập nhật local DMN path trong:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/dmn/CustomerAgeDmnDecisionService.java
```

Từ:

```text
dmn/customer-age-eligibility.dmn
```

Sang:

```text
dmn/customer-kyc-rules.dmn
```

Decision ID `customerAgeEligibility` vẫn giữ nguyên để không ảnh hưởng logic hiện tại và cấu hình Camunda SaaS.

Đã cập nhật comment cấu hình trong:

```text
backend/loan-onboarding/src/main/resources/application.properties
```

Đã cập nhật tài liệu DMN:

```text
backend/loan-onboarding/src/main/resources/dmn/README.md
```

## Kiểm tra

Đã chạy test DMN local:

```bash
mvn -Dtest=CustomerAgeDmnDecisionServiceTest test
```

Kết quả:

```text
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Đã chạy clean compile:

```bash
mvn clean compile
```

Kết quả:

```text
BUILD SUCCESS
```

Sau `mvn clean compile`, thư mục build output:

```text
backend/loan-onboarding/target/classes/dmn
```

chỉ còn đúng 4 file DMN mới.

