# Work Log - 2026-07-06

Tài liệu này ghi lại các thay đổi trong ngày 2026-07-06 để phục vụ audit khi người khác lấy code về kiểm tra hoặc tiếp tục phát triển.

## 1. Gom thiết kế database mới vào Flyway V9

### Bối cảnh

Folder `database/migrations` và `database/seed` đã có nhiều file mới hơn so với Flyway migration trong backend.

Trước thay đổi, backend mới có:

```text
backend/loan-onboarding/src/main/resources/db/migration/V1..V8
```

Yêu cầu:

```text
Tạo V9 trong resource db/migration để Flyway chạy được thiết kế DB mới nhất.
```

### File đã thêm

```text
backend/loan-onboarding/src/main/resources/db/migration/V9__add_kyc_income_and_draft_flow_schema.sql
```

### Nội dung đã gom vào V9

Migration:

```text
database/migrations/V18__add_kyc_profile.sql
database/migrations/V19__update_loan_application_product_and_income_source.sql
database/migrations/V20__add_registration_certificate_number_to_asset.sql
database/migrations/V21__add_loan_application_draft_step_flow.sql
database/migrations/V22__simplify_loan_application_draft_step_flow.sql
database/migrations/V23__remove_unused_kyc_profile_fields.sql
database/migrations/V24__refine_loan_application_draft_review_flow.sql
```

Seed:

```text
database/seed/V11__seed_income_source.sql
database/seed/V12__seed_loan_application_step.sql
database/seed/V14__seed_loan_application_draft_review_flow.sql
```

### Schema mới chính

V9 thêm/cập nhật:

```text
kyc_profile
income_source
loan_application.loan_product_id
loan_application.income_source_id
asset.registration_certificate_number
loan_application_step
loan_application_draft
loan_application_draft_step_data
loan_application_draft_history
```

### Demo seed

V9 seed:

```text
income_source catalog
loan_application_step catalog
loan_application_draft review-flow demo data
```

## 2. Cập nhật entity/repository/API tối thiểu theo schema mới

### Asset

File:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/Asset.java
```

Thêm field:

```text
registrationCertificateNumber -> asset.registration_certificate_number
```

### LoanApplication

File:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/LoanApplication.java
```

Thêm mapping:

```text
loanProduct -> loan_application.loan_product_id
incomeSource -> loan_application.income_source_id
```

### IncomeSource

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/IncomeSource.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/IncomeSourceRepository.java
```

Mục đích:

```text
Map bảng income_source mới và cho phép lấy danh mục nguồn thu nhập.
```

### Reference Data API

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/controller/ReferenceDataController.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/service/ReferenceDataService.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/service/impl/ReferenceDataServiceImpl.java
```

API mới:

```text
GET /api/v1/reference-data/income-sources
```

Mục đích:

```text
Frontend có dropdown nguồn thu nhập từ seed income_source.
```

## 3. Đối chiếu migration với entity/repository

### Bối cảnh

Sau khi thêm V9, đã kiểm tra lại toàn bộ các bảng được tạo trong Flyway migration để xem backend còn thiếu entity hoặc repository nào.

Các bảng còn thiếu mapping rõ ràng:

```text
kyc_profile
loan_application_step
loan_application_draft
loan_application_draft_step_data
loan_application_draft_history
```

Các bảng join của sản phẩm vay không tạo entity riêng vì đã được map bằng quan hệ Many-to-Many trong `LoanProduct`:

```text
loan_product_purpose
loan_product_score_grade
loan_product_term
loan_product_vehicle_type
```

Lưu ý khi đối chiếu folder `database/migrations`:

```text
database/migrations/V13__add_preliminary_applicant_snapshot.sql có thêm các cột applicant_* vào loan_application.
Tuy nhiên trong luồng schema mới, database/migrations/V19__update_loan_application_product_and_income_source.sql đã bỏ các cột applicant_* này.
V9 trong backend đang phản ánh trạng thái cuối cùng sau V19, nên LoanApplication không map các field applicant_*.
```

### Enum đã thêm

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/enums/LoanApplicationDraftStatus.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/enums/LoanApplicationDraftStepStatus.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/enums/LoanApplicationDraftHistoryAction.java
```

Mục đích:

```text
Chuẩn hóa các trạng thái/action của draft-flow theo CHECK constraint trong migration, tránh dùng String tự do trong entity/service sau này.
```

### Entity đã thêm

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/KycProfile.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/LoanApplicationStep.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/LoanApplicationDraft.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/LoanApplicationDraftStepData.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/entity/LoanApplicationDraftHistory.java
```

Mục đích:

```text
Map đầy đủ các bảng mới của KYC và luồng tạo hồ sơ nháp.
Các cột JSONB như payload/metadata được khai báo bằng JdbcTypeCode(SqlTypes.JSON).
```

### Repository đã thêm

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/KycProfileRepository.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/LoanApplicationStepRepository.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/LoanApplicationDraftRepository.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/LoanApplicationDraftStepDataRepository.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/repository/LoanApplicationDraftHistoryRepository.java
```

Mục đích:

```text
Chuẩn bị tầng truy vấn JPA chuẩn cho các service workflow tiếp theo.
```

### Reference Data API bổ sung

Files:

```text
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/controller/ReferenceDataController.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/service/ReferenceDataService.java
backend/loan-onboarding/src/main/java/com/f88/loanonboarding/service/impl/ReferenceDataServiceImpl.java
```

API mới:

```text
GET /api/v1/reference-data/loan-application-steps
```

Mục đích:

```text
Cho phép UI/backend test lấy danh mục các bước tạo hồ sơ vay từ bảng loan_application_step đã seed.
```

## 4. Gom nhóm DMN để import lên Camunda Web Modeler

### Bối cảnh

Các file DMN ban đầu được viết theo dạng mỗi file một Decision Table để dễ test riêng từng rule.

Khi thao tác trên Camunda Web Modeler, cách phù hợp hơn là gom các Decision Table cùng nhóm nghiệp vụ vào chung một DMN Diagram.

### File đã thêm

```text
backend/loan-onboarding/src/main/resources/dmn/customer-rules.dmn
backend/loan-onboarding/src/main/resources/dmn/asset-rules.dmn
backend/loan-onboarding/src/main/resources/dmn/loan-request-rules.dmn
backend/loan-onboarding/src/main/resources/dmn/asset-valuation-rules.dmn
backend/loan-onboarding/src/main/resources/dmn/loan-product-recommendation-rules.dmn
```

### Cách gom nhóm

```text
customer-rules.dmn:
- customerAgeEligibility
- customerBlacklistCheck

asset-rules.dmn:
- assetRequiredCheck
- assetDuplicateCheck

loan-request-rules.dmn:
- loanPurposeEligibility
- loanTenureEligibility
- requestedAmountLimit

asset-valuation-rules.dmn:
- assetValuationDeductionLimit
- loanableAmountCheck
- ltvLimitCheck

loan-product-recommendation-rules.dmn:
- loanProductPurposeEligibility
- loanProductAssetTypeEligibility
- loanProductTenorEligibility
- loanProductScoreEligibility
- loanProductMinAmountEligibility
```

### Lưu ý kỹ thuật

```text
Giữ nguyên decisionId để backend sau này vẫn gọi đúng decision.
Prefix các id kỹ thuật bên trong từng decision table để tránh trùng id khi nhiều decision nằm cùng một XML.
```

### Tài liệu DMN đã cập nhật

```text
backend/loan-onboarding/src/main/resources/dmn/README.md
```

## 5. Kiểm tra

Đã chạy:

```bash
mvn compile
```

Kết quả:

```text
BUILD SUCCESS
```

Số source Java compile sau cập nhật mới nhất:

```text
163 source files
```
